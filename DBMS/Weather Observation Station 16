Query the smallest Northern Latitude (LAT_N) from STATION that is greater than 38.7780. Round your answer to 4 decimal places.






SOLUTION:
select round(LAT_N,4)
from STATION
where LAT_N > 38.7780           //Select the LAT_N values larger than 38.7780, order them in ascending order and limit the number of values to be 1
order by LAT_N
limit 1;
