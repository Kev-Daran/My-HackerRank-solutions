Query the average population for all cities in CITY, rounded down to the nearest integer.




SOLUTION:
select round(avg(POPULATION))           //round rounds the number off and putting the avg function inside to get the average of all population
from CITY;
