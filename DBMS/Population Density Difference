Query the difference between the maximum and minimum populations in CITY.




SOLUTION:

select (max(POPULATION) - min(POPULATION))                // difference between max population and min population
from   CITY;
